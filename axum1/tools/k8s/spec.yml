apiVersion: v1
kind: Pod
metadata:
  name: axum-server
  labels:
    name: axum-server
    env: dev
spec:
  containers:
    - name: axum-server
      image: <Image>-TODO
      resources:
        limits:
          memory: '128Mi'
          cpu: '500m'
      ports:
        - containerPort: 3000
